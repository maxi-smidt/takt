[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "takt"
version = "0.3.0"
description = "Offline Feuerwehr-Zeitnahme für Raspberry Pi und Laptop"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
dependencies = []

[project.optional-dependencies]
desktop = ["PySide6-Essentials>=6.6,<7"]
gpio = ["gpiozero>=2.0,<3"]
server = ["aiohttp>=3.10,<4"]
registry = ["aiohttp>=3.10,<4", "asyncssh>=2.18,<3", "cryptography>=43,<50"]
dev = [
  "aiohttp>=3.10,<4",
  "asyncssh>=2.18,<3",
  "cryptography>=43,<50",
  "PySide6-Essentials>=6.6,<7",
  "pytest>=8,<9",
  "pytest-qt>=4.4,<5",
  "ruff>=0.9,<1",
]

[project.scripts]
takt = "takt.main:main"
takt-server = "takt.server_main:main"
takt-agent = "takt.management.agent:main"
takt-registry = "takt.registry.server_main:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
"takt" = ["assets/*.wav", "assets/*.mp3"]
"takt.web" = ["static/**/*"]
"takt.registry" = ["static/**/*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"

[tool.ruff]
target-version = "py311"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
